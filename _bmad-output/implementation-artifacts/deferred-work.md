# Deferred Work

- source_spec: `_bmad-output/implementation-artifacts/spec-6-1-admin-panel-entry-point-role-safe-resolution.md`
  summary: The age gate skips its server re-render while the URL carries server-derived modal state (`paywall`, `auth_required`, `auth_error`), so the confirmation itself leaves the header without the admin entry; the entry appears on the next navigation, which the paywall dismissal performs automatically (`paywall-modal.tsx` `router.replace("/")`). Measured on the built worker 2026-09-21: after confirming on `/?auth_required=true` the entry count is 0 and after dismissing the modal it is 1, with no document reload — so the residual is narrower than "until a manual reload": it only affects a visitor who leaves the modal open, and the modal covers the header meanwhile. Covering the intermediate state would need a shared dismissal signal (cross-component plumbing) or param stripping before the refresh, not a wider refresh, which resurrects dismissed dialogs. Reopen trigger: the paywall modal gains a dismissal signal, or an admin reports a missing entry with no modal on screen.
  evidence: Story 6.1 review (verification-gap layer, second finding; edge-case layer, claim 2). Measured regression that forced the guard: `e2e/portal.spec.ts` dismiss tests failed ~1 in 3 runs with an unconditional refresh.
- source_spec: `_bmad-output/implementation-artifacts/spec-6-1-admin-panel-entry-point-role-safe-resolution.md`
  summary: Nothing detects drift between the committed `src/lib/engine-mock.generated.ts` and its sources (`src/engine-plugins/mock-shell.html`, `Ropoductions_WebBridge.js`). The e2e harness now regenerates it through `prebuild` (the e2e server command runs the build), so CI is covered, but a commit that edits the plugin sources without regenerating leaves a stale generated file on the branch. Reopen trigger: next engine-plugin source change.
  evidence: Story 6.1 review (verification-gap layer, third "other finding"); `git ls-files` shows the generated file is tracked and not gitignored.
- source_spec: `_bmad-output/implementation-artifacts/spec-6-1-admin-panel-entry-point-role-safe-resolution.md`
  summary: No e2e assertion covers real session-cookie attributes. Every spec mints cookies with Playwright `addCookies({ name, value, domain, path })`, so `HttpOnly`, `SameSite=Lax`, and the `Secure`-outside-loopback rule that `serializeCookie` applies are never observed end to end; only unit tests cover serialization. Reopen trigger: next change to cookie issuance (`issueSessionResponse`) or a production cookie incident.
  evidence: Story 6.1 review (blind-hunter finding 8, second half); the attributes are unit-covered in `tests/cookies.test.ts` but no browser ever receives them.
- source_spec: `_bmad-output/implementation-artifacts/spec-6-1-admin-panel-entry-point-role-safe-resolution.md`
  summary: `/play` validates the caller's session twice per render — `play/page.tsx` calls `validateSessionAccess` for the paywall/tier decision and `resolveAdminAccess` calls it again for the chrome verdict. The deferral originally called this "a duplicate read"; it is more than that, because `validateSessionAccess` is not read-only: it revokes on `override_deleted` (`src/lib/auth.ts:140`) and elevates via `upsertSession` (`:171`), so an authorized render performs two side-effecting validations and up to five D1 round-trips for an admin-role session. The resolver's frozen signature takes only a cookie reader, and `src/lib/auth.ts` is outside Story 6.1's Files list, so the cost stays recorded rather than refactored. Reopen trigger: `/play` TTFB regresses, Story 6.2 adds a third caller, or an in-isolate session/override cache lands for the asset route (spec-3-2 precedent).
  evidence: Story 6.1 pre-implementation roundtable (Mary/Amelia raised, Winston/Amelia adjudicated to keep the spec sequence); recorded in the spec's Spec Change Log item 5.
- source_spec: `_bmad-output/implementation-artifacts/spec-6-1-admin-panel-entry-point-role-safe-resolution.md`
  summary: `tests/admin-entry.test.ts` has two behavioral holes behind its 20 green cases: no resolver case for a lapsed (expired) session holding a valid admin override — only revoked and unknown ids are seeded, so a regression mapping `lapsed` to an admin verdict stays green — and no exercise of the React `cache()` request-level dedupe, because the suite runs outside a dispatcher where `cache` is a passthrough, so removing the wrap would leave every case passing. Rendering coverage itself is not missing: `e2e/admin-entry.spec.ts` asserts the real DOM on the built worker.
  evidence: Story 6.1 second review round (code-review layer, P2 findings at `tests/admin-entry.test.ts:368-378` and `:487-491`).
- source_spec: `_bmad-output/implementation-artifacts/spec-6-1-admin-panel-entry-point-role-safe-resolution.md`
  summary: `open-next.config.ts` is matched by neither the portal nor the admin e2e path filter in `.github/workflows/e2e.yml`, so a change to the OpenNext build config does not trigger the e2e job that now depends on `npm run build`.
  evidence: Story 6.1 second review round (verification-gap layer); `scripts/**`, `migrations/**` and `wrangler.toml` were added by this PR, `open-next.config.ts` was not.
- source_spec: `_bmad-output/implementation-artifacts/spec-6-1-admin-panel-entry-point-role-safe-resolution.md`
  summary: `tests/verify-d1-schema.ts:457` clears every admin override from the shared local D1 (`DELETE FROM patron_overrides WHERE role = 'admin'`) as a precondition for its sole-admin step, and it runs against the same `.wrangler/state/v3` file that `npm run dev`, the e2e harness and anyone's bootstrapped founder row use. Observed during this review: running `npm test` after the e2e suite removed the seeded panel-admin and pledging-admin overrides, leaving only the comp fixture, and `GET /` then rendered no admin entry for a pledging-admin session. Owner invariant (2026-09-21): DELETE and MODIFY must never be applied to a founder admin. Fix direction: isolate the contract suite's D1 state (`wrangler ... --persist-to` + `getPlatformProxy({ persist })`) or scope the purge to the ids the script itself created. Belongs on its own `fix/` branch — the file is untouched by Story 6.1.
  evidence: Story 6.1 second review round (orchestrator probe: `patron_overrides` held 1 of 3 seeded rows after `npm test`; security layer HARNESS-FOUNDER-GUARD-01 is the same local-state hazard).
- source_spec: `_bmad-output/implementation-artifacts/spec-5-2-patron-override-directory-creation-interface.md`
  summary: Normalize the sealed-admin `granted_by` sentinel: Story 2.2 promises the OAuth callback writes `creator_bootstrap`, but `src/lib/patreon.ts` bootstrap/sync paths write `system_bootstrap`; `isSealedCreatorAdmin` checks both so behavior is correct, but the `creator_bootstrap` literal is dead code (spec drift).
  evidence: Security review of Story 5.3 (RPD-5.3-03); belongs to Story 2.2/5.2, not 5.3.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-paywall-interstitial-graceful-navigation.md`
  summary: Enforce revoked/lapsed sessions on `/api/game/[...asset]` streaming requests, not only on `/play` navigation.
  evidence: Story 2-4 covers `/play` and portal interstitials; asset-route session enforcement is already scoped to Epic 3 story 3-2.
  resolution: CLOSED 2026-09-19 (epic-5 retro verification) — `/api/game` route executes `validateSessionAccess` (`src/app/api/game/[[...asset]]/route.ts:7,225`), which revokes sessions on revoked flag, expiry, and deleted-override per Story 2.4.
- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-save-export-review-fixes.md`
  summary: Localize the save-export fallback error string across EN/JA/ES/RU/ZH/PL instead of hardcoded English.
  evidence: Blind review confirmed `setExportError(error instanceof Error ? error.message : "Save export failed")` bypasses next-intl; engine error text is dynamic English anyway, so a locale key is cosmetic until error copy is specified.
- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-save-export-review-fixes.md`
  summary: Fix `.gitignore` `!.github/workflows/*` negation which needs a preceding `!.github/` re-include to take effect.
  evidence: Blind review flagged gitignore negation semantics; the lines come from the synced local file, not this change, so left untouched to avoid altering user intent.
- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-save-export-review-fixes.md`
  summary: Add fake-timer coverage for the 60s download-URL revoke and dock exportingRef/mountedRef transitions.
  evidence: Blind review asked for timer/ref behavior tests; per testing guidelines these assert implementation details rather than behavior, so deferred until a jsdom render harness exists.
- source_spec: `_bmad-output/implementation-artifacts/epic-5-retro-2026-09-19.md`
  summary: 4-eyes (second-person approval) for `admin`-role grants. Formally recorded NON-GOAL for v1: founder access is env-guaranteed (Amendment A-2026-09-19-01), one panel admin minting admins is observable once Story 5.5 lands its audit trail, and a co-sign state machine is a UX cost this studio does not carry yet. Reopen trigger: more than a handful of operators hold admin keys.
  evidence: Epic 5 retro Team Discussion (Winston/John convergence; adversarial lens #8); owner decisions 2026-09-19.
- source_spec: `_bmad-output/implementation-artifacts/epic-5-retro-2026-09-19.md`
  summary: Extract the thrice-duplicated Radix destructive-confirm dialog shell (save-reset, save-import, revoke-override) into one shared component; current copies are byte-identical except max-w. Deferred until a fourth consumer or a styling change forces it, to avoid touching two shipped epic-4 dialogs inside epic-5 scope.
  evidence: Finding F13 (DupSizeViews); dialog shell at revoke-override-button.tsx:84-87 == save-reset-dialog.tsx:100-103 == save-import-dialog.tsx:170-173.
- source_spec: `_bmad-output/implementation-artifacts/epic-5-retro-2026-09-19.md`
  summary: Override directory is capped at 100 rows (`overrides/page.tsx:16`) with no overflow signal; pagination or an N-of-M label rides Story 5.5's trail view. Reopen trigger: first page overflow.
  evidence: Edge-case lens finding 4.
- source_spec: `_bmad-output/implementation-artifacts/epic-5-retro-2026-09-19.md`
  summary: "Date Added" renders UTC (`overrides-table.tsx:72`); label the column or render viewer-timezone once Story 5.4 touches the table.
  evidence: Edge-case lens finding 3.
- source_spec: `_bmad-output/planning-artifacts/analytics/analytics-epic-6-2026-09-21.md`
  summary: Reconcile the stale `public/engine/**` byte-identical mirror contract for the engine plugin (named in `AGENTS.md` and still enforced by the epic-4 retro item-4 wording): the directory does not exist on `develop` — only stale, gitignored copies linger inside `.worktrees/*` — consistent with PR #47 moving MZ shell delivery to the R2 `engine/` prefix streamed via `/engine/[...path]`. Update the repository-rule text (and anything checksumming the mirror) to the R2-served reality. Explicitly NOT Epic 6 scope per the epic-6 brief's out-of-scope list. Reopen trigger: next engine-plugin touch requires a live mirror rule or its removal.
  evidence: Epic 6 analysis §6.3 (known consequences); verified 2026-09-21 that `public/engine` is absent at the `develop` tip while `AGENTS.md` shell-delivery rules describe the R2 `engine/` prefix sync.
  resolution: CLOSED 2026-09-21 (Story 7.1 — the engine-plugin touch that reopened it). No live mirror rule remains to reconcile: `AGENTS.md` carries no `public/engine` reference and states shell delivery as the R2 `engine/` prefix streamed same-origin via `/engine/[...path]`, `public/` holds only `branding/`, `favicon.webp` and `media/`, and `tests/workflow-manifest.test.ts:67-69` fails any workflow that re-introduces a `public/engine` reference. Nothing checksums a mirror; the single-source contract that does exist (`tests/engine-bridge.test.ts` byte-identity over `src/lib/engine-mock.generated.ts` vs `src/engine-plugins/*`) is intact.
- source_spec: `_bmad-output/planning-artifacts/analytics/analytics-epic-6-2026-09-21.md`
  summary: Branded 404 page. Owner may want a studio-branded 404 later; explicitly not Epic 6 scope. Under owner decision Q9 (no wall) every non-admin request for `/admin` returns the stock Next.js 404, so any custom 404 body must preserve the status-code-only semantics pinned by `e2e/admin.spec.ts`: HTTP 404, no `Location` header, no visitor-state differentiation that would create an enumeration oracle.
  evidence: Epic 6 brief out-of-scope list; rejected alternatives D1/D2 (analytics §5.3) showed a shared 404 body changes the response of every unmatched URL and a check inside a streamed shell degrades to a 200 soft-404.
- source_spec: `_bmad-output/implementation-artifacts/spec-7-3-burst-safe-session-validation-for-engine-and-asset-requests.md`
  summary: `validateSessionAccess` can resurrect a concurrently revoked session: the elevation branch builds `elevatedSession` from the row read at `src/lib/auth.ts:111` (where `revoked` was 0), so a revoke that lands between that read and `upsertSession` is overwritten by the whole-record write at `src/lib/db.ts:68-70`. Pre-existing and independent of Story 7.3 (burst amortisation makes the write rarer, not more frequent), and the window is milliseconds. Reopen trigger: the next change to `upsertSession` or the elevation branch, or any revocation incident where access outlived the revocation.
  evidence: Story 7.3 pre-implementation roundtable (EdgeCaseHunter, second follow-up).
- source_spec: `_bmad-output/implementation-artifacts/spec-7-2-release-addressed-immutable-engine-shell-delivery.md`
  summary: A shell document already parsed when the pipeline replaces the tree could pair its new references with subresource bytes from the previous release (the `aws s3 sync` overwrites objects one by one). With a single unordered sync that window was not self-healing: an addressed subresource response is cached `private, immutable, max-age=31536000`, so the mix would persist for the identifier's one-year lifetime, not only until the next document load. Closed by the now-implemented two-phase publish — every shell object except `index.html` first, `index.html` last — pinned by `tests/workflow-manifest.test.ts`. Reopen trigger: the pipeline collapses the two phases back into one sync, or a pipeline change that deletes before uploading (which would open a real 404 window instead of a mixed-bytes window).
  evidence: Story 7.2 roundtable (originally accepted as residual) corrected by the Epic 7 retrospective: the accepted "bounded and self-healing" description ignored immutable addressed caching, so the workflow now publishes the document last. The sync stays additive (`--exclude` only, never `--delete`), so no prefix is ever emptied.
- source_spec: `_bmad-output/implementation-artifacts/spec-7-2-release-addressed-immutable-engine-shell-delivery.md`
  summary: Media and `data/` are never release-pinned, so a rollback pairs a pinned shell with whatever media the additive sync last wrote. Pre-existing and inside the story's own carve-out (the AC keeps media release-agnostic on purpose). Related, and sharper: the IndexedDB save namespace derives from `data/System.json`'s `advanced.gameId`, so an upstream change to that value orphans player saves independently of this story. Reopen trigger: the first upstream `gameId` change, or a media/shell skew report after a rollback.
  evidence: Story 7.2 spec (AC "media keeps its existing moderate private cache"), owner directive OD-2b item 3.
- source_spec: `_bmad-output/implementation-artifacts/spec-7-2-release-addressed-immutable-engine-shell-delivery.md`
  summary: Pre-existing pipeline hygiene, all outside this story's scope: the upstream shell's `cordova.js` reference is dangling (the file is not in the staged tree), `languages/` is never staged, and upstream `.bak` files ship inside the published shell. Reopen trigger: the next ingestion-engine change.
  evidence: Story 7.2 roundtable (rejected as in-scope work, recorded as residual).
- source_spec: `_bmad-output/implementation-artifacts/spec-7-2-release-addressed-immutable-engine-shell-delivery.md`
  summary: The shell route still spends one R2 `head` plus one `get` per file (the `head` supplies ETag, size and content type for conditional and range handling). Halving that to a single `get` with a body-derived ETag is a candidate follow-up, not a correctness issue. Reopen trigger: an R2 cost or per-request latency complaint on the engine shell.
  evidence: Story 7.2 review of `src/app/engine/[...path]/route.ts` (pre-existing shape, unchanged by this story).
